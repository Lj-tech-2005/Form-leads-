{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon index.js",
    "build": "cd ../frontend && npm install && npm run build",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
